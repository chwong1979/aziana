# AGENTS.md — Aziana (Odarius suite)

Pointer file, **not** a source of truth. It routes any coding agent (Codex, ChatGPT,
GLM/OpenCode, Claude, Copilot) into the Odarius system. Don't add canonical rules here —
they live in this repo's own docs and in the command center.

## Read first, in order
1. **This repo:** `START_HERE.md` -> then the docs it links (README / deploy / handoff / project log).
2. **Command center** -- `chwong1979/odarius-governance`:
   `docs/GOVERNANCE.md` (suite map + non-negotiables), `docs/CHIN_PREFERENCES.md`
   (how Chin works), `docs/APP_REGISTRY.md` (app list),
   `docs/backlog/MASTER_BACKLOG.md` (shared roadmap -- read at start, append app-tagged).

## After work
Per `GOVERNANCE.md`, update **both** layers: this repo's docs **and** the command center
(registry / backlog / handoff). No cross-push between repos. The repo's `START_HERE.md` / project
log wins over your memory. Surface real forks before building; never claim browser-verified;
stop and ask before anything irreversible.
